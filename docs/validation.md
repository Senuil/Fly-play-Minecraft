# 本次驗證記錄

環境：Linux x86_64、Python 3.12.14、NumPy 2.3.5、SciPy 1.17.0、PyArrow 23.0.1、
Node 24.19.0、Mineflayer 4.39.0、Flying Squid 1.12.0。日期 2026-09-11 UTC。
這是工作環境的實測，不是使用者的 RTX 5070 筆電。

| 驗證 | 結果／範圍 |
|---|---|
| 合成神經電路 | 已確認活動傳播、無輸入靜默、相同 seed 重現、切斷權重使動作消失 |
| 真實六神經元 fixture | 真實資料匯入完成；前進、左右轉與消融測試 |
| 資料 | root ID 字串精度、矩陣方向、權重正負、多筆邊加總 |
| HTTP | 真正 Node → Python HTTP；session、序號、輸入驗證；模擬 bot 執行器 |
| Minecraft 網路 | 正式 bridge 入口登入實際 Flying Squid 伺服器，使用真實六神經元 fixture |
| Minecraft 1.21.9 / Paper | 尚未測試；目前實際整合是 1.16.5 協定 |
| Windows、RTX 5070、CUDA | 尚未實測；本版只實作 CPU |
| Brian2 科學對照 | 尚未執行 |

Minecraft 成功測次：伺服器收到 11 筆 position/look 類封包，水平最大位移
1.1626180745 格、yaw 範圍 24.45000076 度。這不是假 bot 的方法呼叫測試。
第一個伺服器 fixture 曾因出生高度落在草地內而失敗；修正地表高度後通過。
初次分開啟動 HTTP 服務的測試也曾因程序未持續存活而失敗，
已改成同一測試 runner 管理服務生命周期並通過跨程序測試。

## 效能數值

每 step 模擬 50 ms，內部 dt=0.1 ms；使用固定 probe 感覺刺激。
無 warm-up 排除，含第一次 step；不含資料載入時間。不代表精確的長時間穩態評測。

| 模型 | Steps | neurons / edges | p50 | p95 | 神經時間／實際時間 |
|---|---:|---|---:|---:|---:|
| 合成 probe | 50 | 6 / 3 | 9.979 ms | 14.894 ms | 4.643 |
| 真實 probe | 50 | 6 / 6 | 9.863 ms | 14.198 ms | 4.825 |
| 完整模型 | 3 | 138,639 / 15,091,983 | 11,077.708 ms | 11,245.313 ms | 0.004795 |

完整模型僅三個神經窗，p95 是少量樣本的描述值，不足以證明長期穩定性。
完整 CSR 權重實際計算佔 121,290,424 bytes；沒有把矩陣大小誤稱為程序峰值 RAM。
probe 可在目前環境達 20 Hz；完整 CPU 基線不行。接上慢全腦服務會逾時停止，屬預期保護。

重跑指令見 README；最終測試輸出另存 `test-results.txt`。
